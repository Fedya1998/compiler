# A simple translator from my own language to nasm assembly language (use ASM_MODE) or to processor opcodes (use BIN_MODE)
f_bin() is similar to f_asm() so you can compare them to see what is going on
